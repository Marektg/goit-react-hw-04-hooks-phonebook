import React from "react";
const ContactList = ({ renderHandler }) => {
    
  
        return (
            <ul>
                {renderHandler}
            </ul>
        )

};

export default ContactList;